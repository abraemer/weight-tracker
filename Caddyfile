# Sample Caddyfile for weight-tracker.
#
# The container serves both the frontend (static files) and the API on one
# port, so the proxy just forwards everything. Use localhost:<port> when
# Caddy runs on the host, or the compose service name when Caddy shares a
# Docker network with the app.

weight-tracker.example.com {
	reverse_proxy localhost:3000
}
